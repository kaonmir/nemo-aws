apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: boutique.nemo.kaonmir.xyz
  name: business-gateway
  namespace: default
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - boutique.nemo.kaonmir.xyz
      port:
        name: http
        number: 80
        protocol: HTTP
