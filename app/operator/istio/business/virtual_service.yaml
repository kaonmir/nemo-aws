apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: boutique
  namespace: default
spec:
  gateways:
    - default/gateway-business
  hosts:
    - boutique.business.kaonmir.xyz
    - whereurl.com
  http:
    - route:
        - destination:
            host: frontend.default.svc.cluster.local
            port:
              number: 80
