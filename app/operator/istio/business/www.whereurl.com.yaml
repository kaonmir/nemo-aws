apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: boutique
  namespace: default
spec:
  gateways:
    - default/gateway-business
  hosts:
    - www.whereurl.com
    - whereurl.com
  http:
    - route:
        - destination:
            host: frontend.default.svc.cluster.local
            port:
              number: 80
